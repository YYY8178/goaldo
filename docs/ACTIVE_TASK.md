# GoalDo 当前活跃任务

> 更新：2026-08-16（Asia/Shanghai）
> 状态：额度接棒，等待产品方向决定
> 正式仓库：`YYY8178/goaldo`
> 公开基线：`main` @ `5758b98`（tag `v0.1.0`）
> 接棒分支：`codex/quota-handoff-20260816`

## 当前目标

先确认 GoalDo 是否继续作为独立产品推进，并定义唯一的下一阶段成果。决定前只允许修复明确的安全、构建或文档阻断，不新增大功能。

## 已完成

- 已发布公开、自部署的 `v0.1.0` 基线。
- README、项目简介、自部署、安全、贡献、许可证和商标边界已进入公开仓库。
- 仓库不包含官方托管数据、真实生产配置或密钥。
- 已建立模型无关的共享上下文、决策和接棒文件。

## 下一步决策

维护者从以下方向选择一个，选择前不并行开发：

1. **继续独立产品**：定义一个 2 周内可验收的真实用户成果与指标。
2. **限定维护**：只修复公开自部署基线的安全、构建和文档问题。
3. **暂停**：保留 `v0.1.0`、问题清单和发布说明，不继续开发。

若选择继续独立产品，再逐文件对账历史本地工作树与公开基线，只把通过隐私、许可证、部署边界和测试审查的差异拆成小 PR。

## 验收标准

- 产品方向、唯一阶段目标、期限和停止条件被明确记录。
- 未经对账，不把历史本地工作树整体推入公开仓库。
- 任何代码变更至少通过：

```bash
npm ci
npm run build
git diff --check
```

- PR 描述明确说明公开代码与官方托管服务的边界。

## 禁止事项

- 不提交真实 `.env`、密钥、用户数据、数据库或官方托管配置。
- 不把公开仓库提交视为部署授权。
- 不合并 `main`、创建新发布或修改生产系统，除非维护者另行批准。
- 不在产品方向未确定时继续堆功能。

## 接棒提示词

```text
继续 GoalDo 的 docs/ACTIVE_TASK.md。先核对 YYY8178/goaldo、当前分支、HEAD 和 git status，读取共享文档并运行基线构建。只从维护者明确选定的下一阶段方向继续；历史本地差异必须逐项审查后拆成小 PR。未经批准，不得合并、发布、部署或接触生产数据/配置。
```
